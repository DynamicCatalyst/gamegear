services:
  chromadb:
    image: ghcr.io/chroma-core/chroma:1.0.0
    environment:
      - IS_PERSISTENT=TRUE
    ports:
      - "8000:8000"
    volumes:
      - chroma-data:/chroma/chroma

volumes:
  chroma-data:
    driver: local