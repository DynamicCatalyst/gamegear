/** Mirrors the backend PagedResponse<T> envelope (inside ApiResponse.data). */
export interface PagedResponse<T> {
  content: T[];
  page: number;
  size: number;
  totalElements: number;
  totalPages: number;
  last: boolean;
}
